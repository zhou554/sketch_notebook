# 网页预览 (preview)

本目录预览页已直接套用仓库根目录的 **`line-minimal-ui.html`**（线条海景 + 半透明壳层 + 可交互 App）。

## 打开

- 双击 `preview/index.html`
- 或在仓库根目录打开 `line-minimal-ui.html`（同源设计）

## 说明

预览为单文件自包含（含海浪 SVG、小鱼/贝壳动效与滑条调参）。Android 端对应视觉：`SeaLineView` 线条海 + 半透明 `shell_scrim` + 线条风控件。
