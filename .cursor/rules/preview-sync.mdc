---
description: Keep the web UI preview in sync when NoteSketch Android app features or UI change
globs: app/src/main/**/*
alwaysApply: false
---

# NoteSketch 网页预览同步

预览：`preview/index.html`（海景底层 + 设置页）。

修改 App 时同步：

1. `SeaSceneView` / `UiPrefs` / `SettingsActivity`
2. 三屏海景底层 + 半透明内容
3. 设置项：内容透明度、海浪高度、起伏、贝壳频率、主题色

## 约定

- 海景全屏底层；内容叠在上方并可调透明度
- 主题色写入 `UiPrefs` 与预览 `NoteThemes`
- 贝壳频率 0 = 关闭
- 尊重 reduced-motion
