---
description: Keep the web UI preview in sync when NoteSketch Android app features or UI change
globs: app/src/main/**/*
alwaysApply: false
---

# NoteSketch 网页预览同步

预览：`preview/index.html`（对齐仓库根目录 `scrapbook-forest-fox-ui.html` 拼贴手账风格）。
贴纸资源：`preview/images/stickers/`。

修改 App 时同步：

1. 方格纸背景、丝带标题、侧栏复习间隔、狐狸 / 森林贴纸
2. 黄 / 粉便签卡片、狐狸 FAB「新建」
3. 新建页黄底手账编辑；详情页白纸 + 时间线
4. 主题色 / 亮度 / 背景类型写入 `UiPrefs`（默认 `forest` + 网格）

## 约定

- 不再使用线条海景 `SeaSceneView` 作为主视觉
- 主页右上角狐狸头进入设置（页面亮度、背景颜色、背景类型：网格/空白/横线/网点）
- 尊重 reduced-motion（预览页）
